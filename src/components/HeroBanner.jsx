import { Col, Container, Row } from "react-bootstrap";

export default function HeroBanner () {
    return(
        <section>
            <Container fluid  classaName= "bg-success">
                <Row>
                    <Col className= "text-center">HI</Col>
                </Row>

            </Container>

        </section>
    )
}